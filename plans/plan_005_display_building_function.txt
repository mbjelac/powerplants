Intro: Each building has a function defined as a set of inputs rpdocing a set of outputs.
For subsequent features it will be important that the user sees a function of the building on the screen.

#1 [done] Display building function
- [done] when building tool is not selected and user clicks on a built building, the building function should be displayed
    - display function should be displayed in the top-right corner of the screen
        - the display canvas is separate from the map canvas, but floating in it's top-right corner
    - display contains:
        - building image & name (like in the building toolbar)
        - building image also contains floor
            - floor in the display has same color as floor on map
        - two columns
            - left: input names
            - right: output names
        - building function is defined in the buildings.md file immediately underneath the building display code block
            - the function syntax is:
                - one or more lines listing inputs, as "InputName <value>"
                - line with equals sign "="
                - one or more lines listing outputs, as "OutputName <value>"
        - column content is centered vertically
        - between columns there is an equals sign centered vertically
    - create visual test which adds a building, then clicks on it to display the building function
- [done] clicking somewhere else hides the function panel but dragging to rotate should not.
    - create visual test which
        - add building
        - click building to display function panel
        - rotate the view (function panel should still be there)

#2 [done] building toolbar also displays function
- [done] when building tool selected display function panel
    - function panel displays only data (not image, floor & name)
    - function panel is displayed to the right of the building toolbar, at the height of the selected tool
    - function panel floats above map canvas
- [done] create visual test which selectes the second building tool

#3 [done] resources have icons next to them
- [done] when displaying inputs and outputs, each item should have an icon between the name & the value
    - resource icons are defined in frontend/src/assets/resources.md
    - each line in file is "ResourceName <icon>"

#4 refactor
- frontend/src/buildingFunctionPanel.ts has duplicate code for creating an input/output element
    - extract & reuse
- frontend/src/toolbar.ts has duplicate code for creating an input/output element
    - extract & reuse
