#1 [done] Increase map
- [done] map should be 20x20
    - [done] adjust creator matrices
- [done] left mouse drag moves the camera over the map

#2 [done] Rivers
- [done] creator creates a river
    - pick a random location on the edge of the map for start of river
    - pick a random location on the edge of the map for end of river
    - a square on the map can either be a river or land
    - creator create a matrix similar to location properties matrices but with 0 & 1
        - 0 = land
        - 1 = river
    - river creation function starts at starting edge square and creates a meandering sequence of 1s through the matrix to reach the end square
    - creator outputs the result into the json file with matrices being output with the row array in one line, second row in the line below, etc.
- [done] river is displayed
    - in frontend when drawing the map locations
        - if a location is a river (river matrix has 1 on that location) draw blue square on floor

#3 Improve river creation
- creator should not create river squares which touch more than 2 other river squares
    - touch means having a neighboring square (same column, row index -1 or +1; same row, column index -1 or +1)
