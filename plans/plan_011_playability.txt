#1 [done] Introducing floating point for location properties modifiers is too complex
- [done] all location property modifiers should be integers
    - modifiers which were generated from 0 to 1 should now be from -6 to 0
    - modifiers which were generated from 1 to 2 should now be from 0 to +6
    - applying a modifier should just sum the value to the value of the output
    - if a negative modifier is applied, the result can be a minimum of 0, not below
    - extract the modifier limits -6 & 6 so they can be easily modified to improve user experience
    - adjust all tests
- [done] positive location property modifiers are displayed with "+" prefix
    - zero modifiers are displayed as just "0"

#2 [done] Buildings which have an import are highlighted
- [done] when a player floats their mouse cursor above an import amount in the state panel
    - all buildings which have the import are highlighted
    - highlight is like the select highlight
        - is orange instead of yellow
    - import row has gray highlight, same color as building panel import row background highlight
- [done] create visual test
    - with several buildings having the same resource type imported
    - cursor hover event on state panel over that imported resource amount

#3 [done] Building function description also displays location properties which affect it
- [done] when a player selectes a building in the toolbar, the panel with function description also has a list of location properties which affect it
    - below the function table
    - list location properties which affect the building
    - each list item has the name of the property and the swatch of the maximum value
- [done] create visual test for that panel only (if it doesn't exist yet)
    - include test with a panel containing a modifier property and one without any

#4 [done] Building panel clearly labels inputs & outputs
- [done] inputs column in the building panel has label "Resource"
    - should be "Inputs"
- [done] outputs column in the building panel has no header
    - create header
        - column with resource name should have label "Outputs"
        - column with output amount should have no label
- [done] add column in output panel for amount of building's output which is exported
    - add BuildingState.exports: ResourceThroughput[]
    - create Sektor test for getting exports of a building
    - display building's exports in the output panel for each resource in it's own sub-column
        - sub-column header should be heroicon arrowUpTrayIcon
    - update snapshots
- [done] building function panel from building toolbar displays both headers
    - currently only output column has header, input column hides it
    - no need to hide input column header

#5 Outputs are displayed on map
- when a building is selected, display all building's outputs on the map using arcs
    - output arcs have the same color as the resource's color (same as for input arcs)
    - output arcs use a dotted line to separate them from output arcs
    - on top of the output arc is a label with the resource icon and amount of the output

