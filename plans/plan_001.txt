#1 [done] get to Architect project baseline
- [done] study what the existing code does
- [done] modify it so that
    - the UI is a horizontal split between a textarea on the left side and the p5 rendering box on the right side
    - the p5 rendering box just renders one box, like the "land" boxes but very thin (low height)
    - ignore the map1.txt file completely and remove all code that processes it
    - remove camera persistance
    - remove edge detection
    - remove any land generation (land, river, mountains)

#2 [done] implement 3-sided pyramid
- [done] when text area changes text, parse it line by line
    - apply every line you understand by drawing something in the p5.js box
- [done] when text area contains line "pyr3" draw a 3-sided pyramid, with it's base touching the top of the floor
    - "floor" means the top side of the already existing box
    - the floor box can not be removed from the structure
- [done] when site opens up, rendering viewport should be tilted so the floor panel is visible from the top at a 45 deg angle, also viewport should be rotated 45 deg to left or right so the floor appears as a rhombus
    - after this change run the single playwright test. it should fail. open the snapshot, then wait for me to confirm is it ok
    - if confirmed, update snapshots
- [done] add playwright test to verify that "pyr3" produces the 3-sided pyramid
- [done] refactor visual.spec.js to remove duplication
    - assert
        - assert section start with waitForTimeout and ends with expect()
        - png file name is the only thing that changes (and only the file name, not extension)
        - this can all be put into a expectScreenshot("name") function
    - setup
        - page.goto & page.waitForSelector("canvas") can be put into beforeEach

#3 [done] object can be moved around
- [done] if after "pyr3" on the same line there is a "translate" command, apply it so that the object is placed to a location relative to it's starting one
    - example: "pyr3 t(-40, 50,120)"
    - "t" means translate
    - arguments in brackets may or may not be separated by spaces but have to be separated with commas
    - arguments are integers from 0 to 100
    - 0 means "do not move"
    - 100 means "move by the amount of floor-width (or height)"
    - 1st argument is moving left-right across the floor
    - 2nd argument is moving forward-backward across the floor
    - 3rd argument is moving up-down
- [done] add visual tests
    - one test for each translation parameter with values -50, 50, -100, 100
    - parameterize tests if test runner library supports it
- [done] refactor sketch.js
    - separate reading of commands and applying them into functions
    - that way the main function can load commands into a const, not val

#4 [done] object can be rotated around it's center
- [done] if after "pyr3" on the same line there is a "rotate" command, apply it so that the object is rotated around it's center
    - example: "pyr3 r(20,-60) t(0, 10, -40)"
    - "r" means rotate
    - rotate command can be on the same line as translate command
    - all commands need to be separated by one or more spaces
    - rotate command has two parameters: angles in degrees, one in a vertical plane, one in horizontal plane
    - angles are integers from -180 to 180
    - 0 means "do not rotate"
    - 180 means "rotate by 180 degrees"
    - r(0, 90) means "rotate by 90 degrees in a vertical plane"
    - r(90, 0) means "rotate by 90 degrees in a horizontal plane"
- [done] add visual tests for rotation
    - parameterized on parameters: r(x, 0), r(0, x) and values x = -40, 40, -90, 90, -175, 175

#5 [done] object can be scaled
- [done] "scale" command looks like "s(100, 10,250)"
    - s function takes 3 parameters, each an integer from 1 to 1000
    - 100 means "do not scale" as it represents the percentage of the original size
    - 10 means "shrink 10 times"
    - 250 means "enlarge 2.5 times"
    - 0 is treated as 1
    - negative numbers are treated as 1
    - 1st parameter is scaling in right-left direction
    - 2nd parameter is scaling in forward-backward direction
    - 3rd parameter is scaling in up-down direction (vertically)
- [done] scale command can have just one parameter
    - if so, apply all 3 directions with the same value
- [done] add visual tests for scaling
    - in their own test file: scale.spec.ts
    - parameterized:
        - s(x, 100, 100)
        - s(100, x, 100)
        - s(100, 100, x)
        - s(x)
        - for x values: -10, 0, 1, 50, 100, 150, 500

#6 [done] 4-sided pyramid
- [done] command "pyr" or "pyr4" produces a 4-sided pyramid
- [done] same modification functions work on it (rotate, translate, scale)
- [done] add visual test for displaying 4-sided pyramid to shapes.spec.js
- [done] add visual tests for displaying multiple pyramids, some 3-sides, some 4-sided
    - to fit, scale them down to 60% and translate each from the center, each in it's own direction

#7 [done] coloring
- [done] color function "c" applies color to the object
    - looks like c(#c23fe0)
    - parameter is a hex rgb value with a # prepended
    - color is applied to the entire body on that line
    - other functions can be on the same line in any order, separated by spaces, like: "pyr t(10,0,100) c(#23fe12) r(45,0)"
- [done] add visual tests colors.spec.ts
    - test pyr3 with a color
    - test multiple pyramids with different colors, scaled to 60% and moved off center each in it's own direction

#8 [done] pyramids with more than 4 sides (5-9)
- [done] implement pyr4, pyr5, etc. until pyr9
    - follow same patterns as for pyr3 and pyr4
- [done] add visual tests for every new pyramid

#9 [done] prisms
- [done] implement pri3, pri4, pri5, etc. until pri9
    - these are prisms with 3 or more sides
    - all functions which are applied to pyramids are also applied to prisms (translate, rotate, scale, color)
- [done] add visual tests for every prism

#10 [done] sphere
- [done] implement sph
    - this renders a sphere
    - all functions which are applied to pyramids are also applied to the sphere (translate, rotate, scale, color)
- [done] add visual tests for a sphere modified by functions

#11 [done] cylinder
- [done] implement cyl
    - this renders a cylinder
    - all functions which are applied to pyramids are also applied to the cylinder (translate, rotate, scale, color)
- [done] add visual tests for a cylinder modified by functions

#12 [done] cone
- [done] implement con
    - this renders a cone
    - all functions which are applied to pyramids are also applied to the cone (translate, rotate, scale, color)
- [done] add visual tests for a cone modified by functions

#13 [done] torus
- [done] implement tor
    - this renders a torus
    - all functions which are applied to pyramids are also applied to the torus (translate, rotate, scale, color)
- [done] add visual tests for a torus modified by functions

#14 [done] wireframe toggle
- [done] implement a UI widget for toggling whether wireframe is visible in the rendering or not
    - functions like a html checkbox
    - looks like a horizontal toggle switch (gray background, white knob)
    - does not have a label "Wireframe visible"; instead has a vector icon which resembles a wireframe of a shape
    - toggles between wireframe and solid
    - is located in the top right corner of the viewport
    - default value is turned on
- [done] add visual test
    - in it's own file "wireframeToggle.spec.ts"
    - populate the canvas with a red sphere
    - turn off wireframe

#15 editor UI
- [done] layout
    - put the text area in the bottom half of the left side
    - above the text area there is a space for the editor panel
- [done] shapes
    - on top of the editor panel there is a "+" button
    - the button adds a new line to the text "pyr"
    - as the line is added, the editor panel also adds a sub-panel underneath the button
    - if there is already a sub-panel, the new one is added underneath it
    - if there are more panels than fit, they scroll (only the subpanels, the "+" button stays visible)
    - a new subpanel has a symbol of a 4-sided pyramid to it's left
- [done] changing shape
    - the pyramid symbol in subpanels is clickable
    - clicking it displays a popup with a list of shapes
    - clicking a shape in the list replaces the current shape in the editor panel and also changes the command in the text area to the selected shape
    - selecting the shape in the 1st subpanel changes the 1st line in the text area
        - selecting the shape in the 2nd subpanel changes the 2st line in the text area
        - and so on
- [done] rotating
    - each subpanel has two slider widgets one beneath the other
    - both sliders input values from 0 to 360
    - when either is changed, update the subpanel's line with the rotate function ("r(x, y)") where x is the value of the top slider and y the value of the bottom one
        - if there is already a rotate function, update it's values
        - if there is no rotate function, add it to the line
    - the sliders have the letter R in front of them vertically centered in the subpanel
    - the sliders do not stretch across the whole subpanel, as there will be more widgets
- [done] translating
    - each subpanel has three slider widgets one beneath the other
    - all sliders input values from -100 to 100
    - all initial values are 0
    - when either is changed, update the subpanel's line with the translate function ("t(x, y, z)") where x, y & z are the values of sliders
        - if there is already a translate function, update it's values
        - if there is no translate function, add it to the line
    - the sliders have the letter T in front of them vertically centered in the subpanel
    - the sliders do not stretch across the whole subpanel, as there will be more widgets
    - each slider has a small "⛌" button next to it
        - clicking the button changes the slider's value to 0
- [done] scaling
    - each subpanel has three slider widgets one beneath the other
    - all sliders input values from 1 to 200
    - all initial values are 100
    - when either is changed, update the subpanel's line with the scale function ("s(x, y, z)") where x, y & z are the values of sliders
        - if there is already a scale function, update it's values
        - if there is no scale function, add it to the line
    - the sliders have the letter S in front of them vertically centered in the subpanel
    - the sliders do not stretch across the whole subpanel, as there will be more widgets
    - each slider has a small "⛌" button next to it
        - clicking the button changes the slider's value to 100
- [done] color
    - each subpanel has a color picker widget
    - the color picker pops up and the color can be selected
    - when color is being selected, it's hex code is displayed
    - when color is selected, the hex code is applied to the subpanel's line with the color function ("c(x)") where x is the hex code
        - if there is already a color function, update it's values
        - if there is no color function, add it to the line
- [done] visual tests
    - write visual tests for using the editor to:
        - create shape and change it to torus
        - create shape and rotate it
        - create shape and translate it
        - create shape and scale it
        - create shape and color it
        - create shape and apply all functions to it
        - create multiple shapes and change them all in various ways
        - create a lot of shapes so their subpanels no longer fit vertically, then scroll through them
- [done] rotation not friendly
    - rotation sliders should have value range -180 to 180
    - initial values are 0
    - each has small "⛌" button next to it
        - clicking the button changes the slider's value to 0
- [done] shape can be scaled in all 3 axes together
    - the three scale sliders have a lock icon next to them, vertically centered in the subpanel
    - initial appearance of the lock icon is unlocked and so is the subpanel editor state scaleLocked=false
    - clicking the lock icon toggles it from unlocked to locked and vice versa, and also flips the value of scaleLocked
    - when scaleLocked is set to true, current slider values are NOT changed
    - when scaleLocked is true and any scale slider is moved, all the sliders are moved together
        - sliders are moved until one is on the limit
        - if one slider is on it's limit no sliders can be moved in that direction anymore
            - example: if slider 2 is 1 and slider 1 & 3 are 50, moving slider 1 and 3 to the left is not possible
            - example: if slider 3 is 200 and slider 1 & 2 are 50, moving slider 1 and 2 to the right is not possible

#16 [done] editor: duplicate
- [done] each editor subpanel has a "duplicate" button
    - clicking the button duplicates the text line of the subpanel and places it beneath the current one
    - the button has an icon with two rectangles overlapping each other
- [done] add visual test for duplicating a line

#17 [done] editor: delete
- [done] each editor subpanel has a "delete" button
    - clicking the button deletes that subpanel and it's line from the text
    - the button has a trashcan icon
- [done] add visual test for deleting a line

#18 [done] updating text area updates editor
- [done] when text area is updated, and the update is valid, update all the editor subpanels to reflect the change
- [done] add visual test
    - add a line with the editor
    - change the text so subpanel is updated

#19 load last text on startup
- when text area is updated, this is automatically stored in browser's local storage
    - on app start, the text area is loaded with stored text
